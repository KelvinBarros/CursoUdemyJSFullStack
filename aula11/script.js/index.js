alert('mensagem de alerta, da ok ai tio');

window.confirm('Confirma a operação?') && console.log('Operação confirmada');

 const num1 = window.prompt('Digite um número')
 const num2 = window.prompt('Digite outro número')

console.log(`Soma: ${Number(num1) + Number(num2)}`);